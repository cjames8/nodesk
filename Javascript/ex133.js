const mutants = ['Professor X', 'Cyclops', 'Iceman', 'Angel', 'Magneto', 'Beast', 'Phoenix', 'Logan', 'Gambit']

for(counter = 0; counter < mutants.length; counter++){
    console.log (mutants);
}
if (muntants == 'Magneto') {
    return;
}
else {
    console.log(mutants);
}
